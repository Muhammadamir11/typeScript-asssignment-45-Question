let sandwiches = ((manufacturer:string, modelName:string, ...arbitraryInfo: {[key:string]:any}) => {
    let Car = {
        carManufacturer : manufacturer,
        carModel : modelName,
        color: arbitraryInfo
    }

})
